###If you want to select role 
UPDATE Users SET Role = 'Staff' WHERE Email = 'their-email@example.com';

###If you want for the multiple role select insert 2 time with same id
-- Find the user's Id first
SELECT Id, Email FROM Users WHERE Email = 'their-email@example.com';

-- Assign a role by inserting a row
INSERT INTO UserRoleAssignments (Id, UserId, Role)
VALUES (NEWID(), 'paste-the-user-id-here', 'Staff');